# Ruff lint config (CI Tier 2 gate).
#
# ruff defaults (E4/E7/E9 pycodestyle errors + F Pyflakes) plus import sorting (I).
# No E501 line-length yet (avoids wrapping churn). `ruff format` enforces
# formatting separately in CI.
target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F", "I"]
