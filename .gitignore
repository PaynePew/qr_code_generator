# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.*
!.env.example
!.env.prod.example
!.env.backup.example
# E2E same-origin config (no secrets — just empties VITE_API_BASE_URL); bead 8vd
!frontend/.env.test

# SQLite
*.db
*.db-shm
*.db-wal

# GeoLite2 country DB — licensed MaxMind data, fetched via geoipupdate, never
# committed (ADR 0016, bd qr_code_generator-bii). The backend/data/ dir is kept.
backend/data/*.mmdb

# Local dev object storage (LocalDiskGateway): customized QR composites + logos,
# written when no AWS_S3_BUCKET is set. Dev-only; prod uses S3. Never committed.
backend/data/storage/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Claude local settings
.claude/

# Local-only docs (learning notes, not committed)
.docs-local/

# node_modules
node_modules/

# Frontend build output
frontend/dist/

# Playwright E2E (bead 8vd): minted-cookie storageState + run artifacts
frontend/e2e/.auth/
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.cache/

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/proxieddb/

# Beads passive export + transient import file
# (issues sync via Dolt refs/dolt/data, not git; .beads/.gitignore deliberately leaves
#  issues.jsonl trackable, but this project treats Dolt as the single source of truth)
.beads/issues.jsonl
.beads/.auto-import-issues.jsonl
